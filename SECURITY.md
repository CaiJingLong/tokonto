# 安全与隐私

Token Usage 在本地保存来源、模型、时间、token 数量和费用等元数据；不保存聊天正文和 API key，不上传账本。诊断信息可能包含本地路径和记录 ID，分享前请脱敏。

服务仅监听 `127.0.0.1`，校验 Host、Origin 和写请求头，不提供远程用户认证。不要通过反向代理或端口转发直接公开服务。

外置 Provider 是以当前用户权限执行的代码，超时和输出限制不构成安全沙箱。只注册可信脚本；`providers test` 也会真实执行脚本。

发现安全问题时，若仓库启用了 GitHub 私密漏洞报告，请通过 Security 页的 Report a vulnerability 提交；否则先与维护者建立私密联系。不要在公开 issue 中提交密钥、个人数据库、原始对话或可直接利用的漏洞细节。普通 bug 可以用脱敏构造样例复现。
